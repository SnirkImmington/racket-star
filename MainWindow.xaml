<Window x:Class="RacketSharp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="500" Width="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="0"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="260" MaxWidth="260" MinWidth="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Name="LabelMain" HorizontalAlignment="Center">racket-sharp editor</Label>
        <Label Name="LabelObjects" Grid.Column="2" HorizontalAlignment="Center">object viewer</Label>
        
        <RichTextBox Name="HistoryBox" Grid.Row="1" Style="{StaticResource HistoryBox}"/>
        
        <!-- This does not work right now.
        <GridSplitter Grid.Column="1" Grid.RowSpan="3" Width="4"/> -->
        
        <TreeView Name="ObjectBrowserTree" Grid.Column="2" Grid.Row="1" Grid.RowSpan="2" Width="Auto" HorizontalAlignment="Stretch"
                  FontSize="13" BorderThickness="0"/>
        
        <RichTextBox Name="InputBox" Grid.Row="2" Style="{StaticResource InputCodeBox}"/>
    </Grid>
</Window>
